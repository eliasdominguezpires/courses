const nombre = "Deadpool";
const real = 'Wade Winston';

const normal = nombre + real;
const template = `Elias Dominguez`;
const templa = `${1+1} lo que sea ${template}`;

console.log(normal);
console.log(template);

const htlml = `
<h1>Hola</h1>
<p>Mundo</p>
`;
